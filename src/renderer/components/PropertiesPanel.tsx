export function PropertiesPanel() {
  return (
    <aside className="panel properties-panel" aria-labelledby="properties-heading">
      <h2 id="properties-heading">속성</h2>
      <p>선택한 항목의 설정이 여기에 표시됩니다.</p>
    </aside>
  );
}